# npm-check-updates configuration.
# The previous wrangler hold (Node >=22) no longer applies: CI runs scripts
# under Bun, whose node-compat satisfies it.
reject:
  # typescript-eslint@8.63.0 (latest) peer dep caps typescript at "<6.1.0".
  # TS 7.0.2 would break linting via ERESOLVE until typescript-eslint adds
  # TS 7 support. Revisit once typescript-eslint raises its typescript range.
  - typescript
  # Intentional exact prerelease pin, preserved across every prior update pass
  # (see PRs #19, #27, #38). Stable 9.1.2 is now published, skipping ~80
  # beta/rc releases since alpha.10 — the createTable/getCoreRowModel API
  # used in AnalyticsDashboard.svelte and admin/urls/+page.svelte needs a
  # dedicated migration check before moving off the alpha, out of scope for
  # a routine dependency pass.
  - '@tanstack/svelte-table'
